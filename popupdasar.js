alert ('Selamat Datang..');
var lagi = true;

while (lagi === true) {
    var nama = prompt ('Masukan Nama:');
    alert ('Hallo ' + nama);

    lagi = confirm ('Coba Lagi?');

}

alert ('Terimakasih' + nama);